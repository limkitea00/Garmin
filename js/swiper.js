window.addEventListener("load", () => {
  const swVisual = new Swiper(".sw-visual", {
    effect: "fade",
    loof: true,
    speed: 1500,
    autoplay: {
      delay: 2500,
      disableOnInteraction: false,
    },
  });
});
